{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Questions Schema",
  "description": "JSON schema for fixed question list with immutable ordering, wording, and punctuation",
  "type": "object",
  "required": ["questions"],
  "properties": {
    "questions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["question_id", "text"],
        "properties": {
          "question_id": {
            "type": "string",
            "pattern": "^Q\\d+_[a-z_]+$",
            "description": "Unique question identifier (e.g., Q1_definition)"
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "Question text (immutable wording and punctuation)"
          },
          "type": {
            "type": "string",
            "enum": ["direct", "indirect", "mirroring"],
            "description": "Question style used for RQ3/RQ4 analyses"
          },
          "diagnostic_for": {
            "type": "string",
            "enum": ["conspiratorial", "secondary", "new", "mixed", "none"],
            "description": "Primary PCA dimension the item is diagnostic for"
          }
        },
        "additionalProperties": false
      },
      "uniqueItems": true
    }
  },
  "additionalProperties": false
}

